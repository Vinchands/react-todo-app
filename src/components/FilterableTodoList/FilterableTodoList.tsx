import './FilterableTodoList.css'

interface FilterableTodoListProps {
    todos: {
        id: number;
        title: string;
        completed: boolean;
    }
}

export default function FilterableTodoList({ todos }: FilterableTodoListProps): JSX.Element {
    return (
        <>
        
        </>
    )
}
